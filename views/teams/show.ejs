<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Show Team</title>
    </head>
    <body>
        <div class="container">
            <div class="content">
                <p>
                    <%= team.name %> - <%= team.division %> - <%= team.founded
                    %>
                </p>
            </div>
            <div class="action">
                <a href="/teams/<%= team.id %>/edit">Edit</a>
                <form
                    action="/teams/<%= team.id %>?_method=DELETE"
                    method="POST"
                >
                    <button type="submit">Delete Team</button>
                </form>
            </div>
        </div>
    </body>
</html>
